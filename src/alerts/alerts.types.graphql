type Mutation {
  donationAlertsSend(
    id: ID!
    username: String!
    amount: Int!
    text: String!
  ): DonationAlert
}

type Subscription {
  newDonationAlert(id: ID!): DonationAlert
}

type DonationAlert {
  id: ID
  username: String
  amount: Int
  text: String
}
