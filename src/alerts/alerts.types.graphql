type Query {
  alerts: [Alert]
}

type Mutation {
  donationAlertsSend(
    id: ID!
    username: String!
    amount: Int!
    text: String!
  ): Alert
}

type Subscription {
  newDonationAlert(id: ID!): Alert
}

type Alert {
  id: ID
  username: String
  amount: Int
  text: String
}
